# Diary — T-FR-0002-01 (parallel)

**Ticket:** Define budget allocation contracts  
**Branch:** `feat/FR-0002-budget-entry-page--T-FR-0002-01-define-budget-allocation-contracts`  
**Worktree:** `.worktrees/FR-0002-budget-entry-page/T-FR-0002-01-define-budget-allocation-contracts/`

---

## 2026-04-30

### Completed

- Added `finance.budget_allocation` with cadence enums, monthly equivalence, payment method enum, and `build_allocation_summary` (category/cadence splits, cash/credit, timing-sensitive cash, remaining income).
- Added ORM tables/models for plans and items; SQL stub `phase2_budget_allocation_stub.sql`.
- Extended `api/schemas.py` with plan/item create/out/summary models and `build_budget_allocation_plan_summary`, avoiding import of `normalize_period_month` from `finance.unified` to prevent a circular import (inlined `_first_day_of_month`).
- Tests cover cadence table, validation, summary math, and SQLite ORM round-trip.

### Validation

- **Docker:** `docker compose run --rm api sh -c "pip install -q pytest && pip install -q -e /app && cd /app && pytest tests/ -q --tb=short"` — 32 passed.

  **Note:** The `api` image does not include `pytest`; tests are run with an inline `pip install pytest` in the one-shot command above.

### Deviations

- Ticket handoff branch naming: Git cannot nest a branch under an existing `feat/FR-0002-budget-entry-page` ref; work continued on `feat/FR-0002-budget-entry-page--T-FR-0002-01-define-budget-allocation-contracts` as established.
